{
  "name": "server-exam",
  "private": true,
  "devDependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "json-server-auth --watch db.json --port 3000"
  },
  "dependencies": {
    "axios": "^1.1.0",
    "json-server-auth": "^2.1.0",
    "react-router-dom": "^6.4.2"
  }
}
